import { Flex } from "@chakra-ui/react";
import MealCard from "./MealCard";

function Content() {
    return (
        <Flex m={5}>
            <MealCard />
        </Flex>
    );
}

export default Content;
